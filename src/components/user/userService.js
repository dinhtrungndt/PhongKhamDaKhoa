import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const userService = () => {
  return (
    <View>
      <Text>userServer</Text>
    </View>
  );
};

export default userService;
